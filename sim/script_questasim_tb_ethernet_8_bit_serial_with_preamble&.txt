quit -sim

if ![info exists QUARTUS_INSTALL_DIR] {
  set QUARTUS_INSTALL_DIR "c:/intelFPGA_lite/22.1std/quartus"
}

if ![info exists PRJ_DIR] {
  set PRJ_DIR "d:/programming/RLCBC_BROD"
}

global QUARTUS_INSTALL_DIR
global PRJ_DIR

cd "$PRJ_DIR/sim"
vlib work
vmap work work

vlog ../tb/ethernet/my_fifo_test/my_fifo_test.v

vsim -voptargs=+acc work.tb_ethernet_top

run -all